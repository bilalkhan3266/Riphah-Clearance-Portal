const Return = require('../../models/Return');
const Issue = require('../../models/Issue');
exports.createReturn = async (req, res) => { try { const { facultyId, referenceIssueId, quantityReturned, condition } = req.body; const issue = await Issue.findById(referenceIssueId); if (!issue) return res.status(404).json({ success: false, message: 'Not found' }); const newReturn = new Return({ facultyId, departmentName: 'HR', referenceIssueId, quantityReturned: quantityReturned || issue.quantity, returnDate: Date.now(), condition: condition || 'Good', status: 'Returned', receivedBy: req.user?.employeeId || 'system' }); await newReturn.save(); if (quantityReturned >= issue.quantity) issue.status = 'Cleared'; else issue.status = 'Partially Returned'; await issue.save(); res.status(201).json({ success: true, message: 'Return in HR', data: newReturn }); } catch (error) { res.status(500).json({ success: false, message: 'Error', error: error.message }); } };
exports.getReturns = async (req, res) => { try { const { facultyId, status } = req.query; let query = { departmentName: 'HR' }; if (facultyId) query.facultyId = facultyId; if (status) query.status = status; const returns = await Return.find(query).populate('referenceIssueId').sort({ returnDate: -1 }); res.status(200).json({ success: true, count: returns.length, data: returns }); } catch (error) { res.status(500).json({ success: false, message: 'Error', error: error.message }); } };
exports.verifyReturn = async (req, res) => { try { const { returnId } = req.params; const returnRecord = await Return.findByIdAndUpdate(returnId, { status: 'Cleared', verifiedBy: req.user?.employeeId || 'system', verificationDate: Date.now() }, { new: true }); if (!returnRecord) return res.status(404).json({ success: false, message: 'Not found' }); res.status(200).json({ success: true, data: returnRecord }); } catch (error) { res.status(500).json({ success: false, message: 'Error', error: error.message }); } };
exports.checkClearanceStatus = async (req, res) => { try { const { facultyId } = req.params; const unclearedIssues = await Issue.find({ facultyId, departmentName: 'HR', status: { $nin: ['Cleared'] } }); res.status(200).json({ success: true, departmentName: 'HR', isCleared: unclearedIssues.length === 0 }); } catch (error) { res.status(500).json({ success: false, message: 'Error', error: error.message }); } };
exports.getStatistics = async (req, res) => { try { const stats = await Return.aggregate([{ $match: { departmentName: 'HR' } }, { $group: { _id: '$status', count: { $sum: 1 } } }]); res.status(200).json({ success: true, departmentName: 'HR', stats }); } catch (error) { res.status(500).json({ success: false, message: 'Error', error: error.message }); } };
