const Issue = require('../../models/Issue');
const autoClearanceService = require('../../services/autoClearanceService');
const DEPT = 'HOD';
exports.createIssue = async (req, res) => { try { const { facultyId, facultyName, facultyEmail, itemType, description, quantity, dueDate, issuedBy, notes } = req.body; const ref = `${DEPT}-${Date.now()}-${Math.random().toString(36).substr(2, 9).toUpperCase()}`; const saved = await new Issue({ facultyId, facultyName, facultyEmail, departmentName: DEPT, itemType, description, quantity: quantity || 1, dueDate, issuedBy, issueReferenceNumber: ref, notes, status: 'Issued' }).save(); res.status(201).json({ success: true, issue: { id: saved._id } }); } catch (e) { res.status(500).json({ success: false }); } };
exports.getFacultyIssues = async (req, res) => { try { const issues = await Issue.find({ facultyId: req.params.facultyId, departmentName: DEPT }).sort({ issueDate: -1 }); res.status(200).json({ success: true, department: DEPT, totalIssues: issues.length, issues }); } catch (e) { res.status(500).json({ success: false }); } };
exports.getAllDeptIssues = async (req, res) => { try { const issues = await Issue.find({ departmentName: DEPT }).sort({ issueDate: -1 }); res.status(200).json({ success: true, department: DEPT, totalIssues: issues.length, issues }); } catch (e) { res.status(500).json({ success: false }); } };
exports.updateIssueStatus = async (req, res) => { try { const updated = await Issue.findByIdAndUpdate(req.params.issueId, { status: req.body.status }, { new: true }); res.status(200).json({ success: true, issue: updated }); } catch (e) { res.status(500).json({ success: false }); } };
exports.checkDepartmentClearance = async (req, res) => { try { const result = await autoClearanceService.checkDepartmentClearance(req.params.facultyId, DEPT); res.status(200).json(result); } catch (e) { res.status(500).json({ success: false }); } };
exports.getStats = async (req, res) => { try { const total = await Issue.countDocuments({ departmentName: DEPT }); const cleared = await Issue.countDocuments({ departmentName: DEPT, status: 'Cleared' }); res.status(200).json({ success: true, department: DEPT, total, cleared }); } catch (e) { res.status(500).json({ success: false }); } };
module.exports = exports;
